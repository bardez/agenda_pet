import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:app_petshop/app/modules/new_schedule/new_schedule_controller.dart';
import 'package:app_petshop/app/modules/new_schedule/new_schedule_module.dart';

void main() {
  initModule(NewScheduleModule());
  // NewScheduleController newschedule;
  //
  setUp(() {
    //     newschedule = NewScheduleModule.to.get<NewScheduleController>();
  });

  group('NewScheduleController Test', () {
    //   test("First Test", () {
    //     expect(newschedule, isInstanceOf<NewScheduleController>());
    //   });

    //   test("Set Value", () {
    //     expect(newschedule.value, equals(0));
    //     newschedule.increment();
    //     expect(newschedule.value, equals(1));
    //   });
  });
}
