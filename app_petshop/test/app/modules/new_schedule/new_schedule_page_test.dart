import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_petshop/app/modules/new_schedule/new_schedule_page.dart';

void main() {
  testWidgets('NewSchedulePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NewSchedulePage(title: 'NewSchedule')));
    //  final titleFinder = find.text('NewSchedule');
    //  expect(titleFinder, findsOneWidget);
  });
}
