import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_petshop/app/modules/container/container_page.dart';

void main() {
  testWidgets('ContainerPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ContainerPage(title: 'Container')));
    //  final titleFinder = find.text('Container');
    //  expect(titleFinder, findsOneWidget);
  });
}
