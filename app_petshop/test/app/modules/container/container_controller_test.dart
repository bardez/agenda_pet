import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:app_petshop/app/modules/container/container_controller.dart';
import 'package:app_petshop/app/modules/container/container_module.dart';

void main() {
  initModule(ContainerModule());
  // ContainerController container;
  //
  setUp(() {
    //     container = ContainerModule.to.get<ContainerController>();
  });

  group('ContainerController Test', () {
    //   test("First Test", () {
    //     expect(container, isInstanceOf<ContainerController>());
    //   });

    //   test("Set Value", () {
    //     expect(container.value, equals(0));
    //     container.increment();
    //     expect(container.value, equals(1));
    //   });
  });
}
