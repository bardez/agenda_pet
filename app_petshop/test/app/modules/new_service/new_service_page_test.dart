import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_petshop/app/modules/new_service/new_service_page.dart';

void main() {
  testWidgets('NewServicePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NewServicePage(title: 'NewService')));
    //  final titleFinder = find.text('NewService');
    //  expect(titleFinder, findsOneWidget);
  });
}
