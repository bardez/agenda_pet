import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_petshop/app/modules/services/services_page.dart';

void main() {
  testWidgets('ServicesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ServicesPage(title: 'Services')));
    //  final titleFinder = find.text('Services');
    //  expect(titleFinder, findsOneWidget);
  });
}
