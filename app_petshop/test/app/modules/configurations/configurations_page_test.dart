import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:app_petshop/app/modules/configurations/configurations_page.dart';

void main() {
  testWidgets('ConfigurationsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ConfigurationsPage(title: 'Configurations')));
    //  final titleFinder = find.text('Configurations');
    //  expect(titleFinder, findsOneWidget);
  });
}
